\section{Method}
SpatialVLM is organized in five stages: dual visual encoding, geometric depth branch, typed fusion,
position routing in the backbone, and staged training (pre-alignment, SFT, GRPO/fDPO). We keep all
pretrained dimensions runtime-verified and isolate each hypothesis via one-module ablations.
