\section{Introduction}
Foundation models excel at semantics yet often fail at indoor spatial reasoning. We hypothesize the
failure is architectural: spatial signals are repeatedly destroyed during encoding, fusion, and
language conditioning. SpatialVLM introduces stage-specific fixes and evaluates them under a strict
ablation protocol centered on navigation and spatial QA.
